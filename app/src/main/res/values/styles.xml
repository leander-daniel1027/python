<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="BadeDialogStyle">
        <item name="android:windowEnterAnimation">@anim/translate_left_to_right_short</item>
        <item name="android:windowExitAnimation">@anim/translate_top_to_bottom_anim</item>
    </style>
    <style name="MyTabLayout" parent="Base.Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/MyTabTextAppearance</item>
    </style>
    <style name="MyTabTextAppearance" parent="TextAppearance.AppCompat.Button">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="TextLabel" parent="TextAppearance.AppCompat">
        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColorHint">@color/colorText</item>

        <!--<item name="font">@string/roboto_regular</item>-->
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="colorAccent">@color/colorText</item>
        <item name="colorControlNormal">@color/colorText</item>
        <item name="colorControlActivated">@color/colorText</item>
    </style>

    <style name="LoginProcessDialog">
        <item name="android:windowEnterAnimation">@anim/enterfrombottom</item>
        <item name="android:windowExitAnimation">@anim/exittobottom</item>
    </style>

    <style name="YourCustomStyle" parent="android:Theme">
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@mipmap/splash_bg</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    </style>

    <style name="normalTab">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="activeTab">
        <item name="android:background">@color/colorWhite</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="AppTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/colorPink</item>
        <item name="tabIndicatorHeight">3dp</item>
        <item name="tabPaddingStart">@dimen/dp10</item>
        <item name="tabPaddingEnd">@dimen/dp10</item>
        <item name="tabBackground">@color/colorPrimary</item>
        <item name="tabSelectedTextColor">@color/colorPink</item>
    </style>
    <style name="checkBoxStyle" parent="Base.Theme.AppCompat">
        <item name="colorAccent">@color/colorPink</item>
        <item name="android:textColorSecondary">#ffffff</item>
    </style>
</resources>
